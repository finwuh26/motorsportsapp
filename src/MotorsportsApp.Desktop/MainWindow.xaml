<Window x:Class="MotorsportsApp.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MotorsportsApp.Desktop"
        mc:Ignorable="d"
        Title="F1 Live Timing &amp; Analytics" 
        Height="900" 
        Width="1600"
        Background="#1E1E1E"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Dark Theme Colors -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#252526"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#E10600"/>
        <SolidColorBrush x:Key="TextBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#CCCCCC"/>
        
        <!-- Styles -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>
        
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
        
        <Style x:Key="SectionStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="20" BorderBrush="{StaticResource AccentBrush}" BorderThickness="0,0,0,3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding Title}" Style="{StaticResource TitleStyle}"/>
                    <TextBlock Text="{Binding CurrentSession.RaceName, FallbackValue='No Active Session'}" 
                               FontSize="16" 
                               Foreground="{StaticResource SecondaryTextBrush}"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentSession.Circuit.CircuitName, FallbackValue=''}" 
                               FontSize="14" 
                               HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Panel - Upcoming Races -->
            <Border Grid.Column="0" Style="{StaticResource SectionStyle}">
                <StackPanel>
                    <TextBlock Text="Upcoming Races" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <ListView ItemsSource="{Binding UpcomingRaces}" 
                              Background="Transparent"
                              BorderThickness="0"
                              Foreground="{StaticResource TextBrush}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#2D2D30" Padding="10" Margin="0,0,0,5" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="{Binding RaceName}" 
                                                   FontWeight="Bold" 
                                                   FontSize="14"/>
                                        <TextBlock Text="{Binding Circuit.CircuitName}" 
                                                   FontSize="12" 
                                                   Foreground="{StaticResource SecondaryTextBrush}"/>
                                        <TextBlock Text="{Binding SessionDate, StringFormat='MMM dd, yyyy'}" 
                                                   FontSize="11" 
                                                   Foreground="{StaticResource SecondaryTextBrush}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>
            
            <!-- Right Panel - Drivers -->
            <Border Grid.Column="1" Style="{StaticResource SectionStyle}">
                <StackPanel>
                    <TextBlock Text="Current Season Drivers" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <ProgressBar Grid.Row="0" 
                                     IsIndeterminate="{Binding IsLoading}" 
                                     Height="4" 
                                     Margin="0,0,0,10"
                                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        
                        <ListView Grid.Row="1"
                                  ItemsSource="{Binding Drivers}" 
                                  Background="Transparent"
                                  BorderThickness="0">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Number" Width="80">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding PermanentNumber}" 
                                                           FontWeight="Bold"
                                                           Foreground="{StaticResource AccentBrush}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Code" Width="80">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Code}" FontWeight="Bold"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Name" Width="250">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding FullName}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Nationality" Width="150">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Nationality}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
